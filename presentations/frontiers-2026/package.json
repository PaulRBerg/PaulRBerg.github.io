{
  "name": "frontiers-2026",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "deploy": "vercel build --prod --token=$VERCEL_TOKEN && vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^52.14.2",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "vue": "^3.5.29"
  },
  "devDependencies": {
    "vercel": "^51.4.0"
  }
}
